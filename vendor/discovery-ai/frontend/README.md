# Frontend (placeholder)

React + Cytoscape.js graph UI, built in Phase 6 (see ../docs/Phases.md). Not scaffolded yet — deliberately left empty until the backend API contract (Phase 6's FastAPI REST/WebSocket endpoints) exists to build against.
